{
  "name": "ecoaldea",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx nodemon --exec 'node index.js -s' --watch src --watch index.js --watch config -e html,css,scss,sass,js,json",
    "build": "npx cross-env NODE_ENV=production node index.js -b",
    "deploy": "npx cross-env NODE_ENV=production node index.js -d",
    "api": "npx cross-env NODE_ENV=production cross-env PORT=3001 node index.js -s -a"
  },
  "license": "MIT",
  "dependencies": {
    "cross-env": "^5.2.0",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "handlebars": "^4.0.12",
    "node-sass": "^4.11.0",
    "nodemon": "^1.18.8",
    "sander": "^0.6.0",
    "yargs": "^12.0.5"
  }
}
